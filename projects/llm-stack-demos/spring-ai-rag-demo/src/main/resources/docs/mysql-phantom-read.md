# MySQL 幻读

幻读指的是同一事务内，两次范围查询看到的结果集行数不一致。

典型原因：

- 第一次范围查询后
- 另一个事务插入了一条落在该范围内的新记录
- 当前事务再次做范围查询时，结果集多出一行

InnoDB 在可重复读下的处理：

- 快照读主要靠 MVCC 保持结果稳定
- 当前读主要靠 Next-Key Lock 防止区间插入

面试表达重点：

- 脏读看未提交
- 不可重复读看同一行值变化
- 幻读看结果集变化
